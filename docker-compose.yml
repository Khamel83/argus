services:
  argus:
    build: .
    ports:
      - "8005:8005"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./argus_budgets.db:/app/argus_budgets.db
    network_mode: bridge
